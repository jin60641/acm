#include<stdio.h>
int main(){
	int N,M;
	scanf("%d %d",&N,&M);
	int arr[101,101];
	for( int i = 0; i < 100; ++i ){
		for( int j = 0; j < 100; ++j )[
			scanf("%d",arr[i][j];
		}
	}
	int X;
	int Y;
	int dir;
	scanf("%d %d %d",X,Y,dir);
	X = X-1;
	Y = Y-1;
	
	
	
	printf("%d",A+B);
	for( int i = 0; i < M; ++i ){
	}
	return 0;
}

int Turn(){
	
}
