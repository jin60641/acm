#include<stdio.h>


int main(){
	int N;
	int cnt = 0;	
	int A[1010];
	scanf("%d",N);
	
	for( int i = 0; i < N; ++i ){
		scanf("%d",A[i]);
	}
	for( int i = 0; i < N; ++i ){
		if( A[i] ){
			for( int j = 2; j < A[i]; ++j ){
				if( A[i]/j ){
					cnt++;
				}
			}
		}
	}
	printf("%d\n",cnt);
}
