#include<stdio.h>

int main(){
	char s[10];
	scanf("%s",s);
	int len = 0;
	for(;s[len]!= 0;++len);
	if( len%2 == 0 ){
		for( int i = len/2-1; i >= 0; --i ){
			if( len-i-1 ){
				
			}
		}
	} else {
	}
	return 0;
}
